# Accrefin

One command runs the loan/marketing website:

```powershell
cd "C:\Users\UDESH\Desktop\accerfin web\accrefin-web"
npm run dev
```

Open **http://localhost:5173**

Resources → Blog is `/blogs` on the same site.

Env file: `accrefin-web/.env`
