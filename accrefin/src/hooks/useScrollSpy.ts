// src/hooks/useScrollSpy.ts (Scroll-based tab highlighting)
import { useState, useEffect } from 'react';
export const useScrollSpy = (sectionIds: string[], offset: number = 150) => { /* ... logic ... */ return activeSection; };