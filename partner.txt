Here are the transcribed codes for the two files shown in your images.

### 1. `LoanFeaturesSection.tsx`

*(Located in `src/screens/Wireframe/sections/LoanFeaturesSection/`)*

```tsx
import {
  Avatar,
  AvatarFallback,
  AvatarImage,
} from "../../../components/ui/avatar";
import { Card, CardContent } from "../../../components/ui/card";

const testimonials = [
  {
    rating: 5,
    text: "Accrefin made my home loan journey incredibly smooth. Got the best rate from HDFC Bank within 24 hours. Highly recommended!",
    name: "Priya Sharma",
    avatar: "https://c.animaapp.com/mm8un0kqTN1UQG/img/image-47.png",
    fallback: "PS",
  },
  {
    rating: 5,
    text: "Quick personal loan approval for my business needs. The team was very professional and transparent about all charges.",
    name: "Rajesh Kumar",
    avatar: "https://c.animaapp.com/mm8un0kqTN1UQG/img/image-47-1.png",
    fallback: "RK",
  },
  {
    rating: 5,
    text: "Excellent service! They helped me compare multiple offers and choose the best one. Saved me ₹50,000 in interest.",
    name: "Anita Patel",
    avatar: "https://c.animaapp.com/mm8un0kqTN1UQG/img/image-47-2.png",
    fallback: "AP",
  },
];

export const LoanFeaturesSection = (): JSX.Element => {
  return (
    <section className="flex flex-col w-full items-center gap-16 py-16 px-4 bg-[#051428]">
      <header className="flex flex-col items-center justify-center gap-5 translate-y-[-1rem] animate-fade-in opacity-0 [--animation-delay:0ms]">
        <h2 className="[font-family:'Power_Grotesk-Regular',Helvetica] font-normal text-white text-4xl md:text-5xl lg:text-[64px] text-center tracking-[0] leading-tight lg:leading-[76.8px]">
          What Our Customers Say
        </h2>
        <p className="[font-family:'DM_Sans',Helvetica] font-medium text-[#a3ccff] text-lg md:text-xl text-center tracking-[-0.40px] leading-[20.8px] max-w-2xl">
          See how Accrefin has helped thousands of customers achieve their financial goals
        </p>
      </header>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-7 w-full max-w-7xl">
        {testimonials.map((testimonial, index) => (
          <Card
            key={index}
            className="flex flex-col h-[401px] items-center justify-between p-8 rounded-lg bg-[linear-gradient(180deg,rgba(8,80,178,1)_0%,rgba(0,34,76,1)_100%)] border-0 translate-y-[-1rem] animate-fade-in opacity-0"
            style={{
              "--animation-delay": `${(index + 1) * 200}ms`,
            } as React.CSSProperties}
          >
            <CardContent className="flex flex-col h-full items-center justify-between p-0 w-full gap-4">
              <div className="flex items-center justify-center gap-3 w-full">
                {Array.from({ length: testimonial.rating }).map((_, starIndex) => (
                  <img
                    key={starIndex}
                    className="w-6 h-6"
                    alt="Star"
                    src="https://c.animaapp.com/mm8un0kqTN1UQG/img/star.svg"
                  />
                ))}
              </div>

              <div className="flex items-center justify-center flex-1 w-full">
                <p className="[font-family:'DM_Sans',Helvetica] font-medium text-white text-base text-center tracking-[-0.32px] leading-[20.8px]">
                  &quot;{testimonial.text}&quot;
                </p>
              </div>

              <div className="flex items-center justify-center gap-3 w-full">
                <Avatar className="w-12 h-12">
                  <AvatarImage src={testimonial.avatar} alt={testimonial.name} />
                  <AvatarFallback>{testimonial.fallback}</AvatarFallback>
                </Avatar>
                <p className="[font-family:'DM_Sans',Helvetica] font-medium text-white text-xl text-center tracking-[-0.40px] leading-[20.8px]">
                  {testimonial.name}
                </p>
              </div>
            </CardContent>
          </Card>
        ))}
      </div>
    </section>
  );
};

```

---

### 2. `card.tsx`

*(Located in `src/components/ui/`)*

```tsx
import * as React from "react";
import { cn } from "../../lib/utils";

const Card = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn(
      "rounded-xl border bg-card text-card-foreground shadow",
      className
    )}
    {...props}
  />
));
Card.displayName = "Card";

const CardHeader = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn("flex flex-col space-y-1.5 p-6", className)}
    {...props}
  />
));
CardHeader.displayName = "CardHeader";

const CardTitle = React.forwardRef<
  HTMLParagraphElement,
  React.HTMLAttributes<HTMLHeadingElement>
>(({ className, ...props }, ref) => (
  <h3
    ref={ref}
    className={cn("font-semibold leading-none tracking-tight", className)}
    {...props}
  />
));
CardTitle.displayName = "CardTitle";

const CardDescription = React.forwardRef<
  HTMLParagraphElement,
  React.HTMLAttributes<HTMLParagraphElement>
>(({ className, ...props }, ref) => (
  <p
    ref={ref}
    className={cn("text-sm text-muted-foreground", className)}
    {...props}
  />
));
CardDescription.displayName = "CardDescription";

const CardContent = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div ref={ref} className={cn("p-6 pt-0", className)} {...props} />
));
CardContent.displayName = "CardContent";

const CardFooter = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn("flex items-center p-6 pt-0", className)}
    {...props}
  />
));
CardFooter.displayName = "CardFooter";

export {
  Card,
  CardHeader,
  CardFooter,
  CardTitle,
  CardDescription,
  CardContent,
};

```

Would you like me to help you create the `Avatar` component as well since it's used in the testimonials?