{
  "buildCommand": "cd accrefin && npm install && npm run build",
  "outputDirectory": "accrefin/dist",
  "installCommand": "true",
  "routes": [
    { "src": "/admin(.*)", "dest": "https://accrefin-web-ten.vercel.app/admin$1" },
    { "src": "/[^.]+$", "dest": "/index.html" }
  ]
}
