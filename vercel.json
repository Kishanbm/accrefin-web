{
  "buildCommand": "cd accrefin && npm install && npm run build",
  "outputDirectory": "accrefin/dist",
  "installCommand": "true",
  "routes": [
    { "src": "/[^.]+$", "dest": "/index.html" }
  ]
}
